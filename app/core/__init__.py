"""
app/core – Application-wide constants and shared primitives.

This package holds values that are referenced across multiple layers
(API, scanner, services) and do not belong to any single feature module.

Contents:
  constants.py – Global constants: app name, version, limits, supported formats.
"""
